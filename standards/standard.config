// Glob is meant for bronze standard - only get number before hyphen
params.sc_input = "standards/reference/${params.standard.split('-')[0]}*.rds"
params.sp_input = "standards/$params.standard/*.rds"
params.annot = "celltype"
params.sampleID = "none"
